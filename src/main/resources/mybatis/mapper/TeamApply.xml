<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goalddae.repository.TeamApplyRepository">
    <update id="createTeamApplyTable" parameterType="java.lang.Long">
        CREATE TABLE team_apply_#{teamId} (
                                       id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                       team_apply_date DATETIME NOT NULL,
                                       team_accept_status BIGINT NOT NULL,
                                       users_id BIGINT NOT NULL,
                                       team_id BIGINT NOT NULL
        );
    </update>
    <insert id="addTeamApply" parameterType="com.goalddae.dto.team.TeamApplyDTO">
        INSERT INTO team_apply_#{teamId} (team_apply_date, team_accept_status, team_id, users_id)
        VALUES (now(), #{teamAcceptStatus}, #{teamId}, #{userId})
    </insert>
</mapper>
