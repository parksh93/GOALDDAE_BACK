<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goalddae.repository.FieldReservationRepository">
    <update id="dropFieldReservationTable">
        DROP TABLE IF EXISTS ${fieldReservation};
    </update>

    <update id="createFieldReservationTable">
        CREATE TABLE ${fieldReservation} (
                                             id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                             reservation_id VARCHAR(255) NOT NULL,
                                             reservation_date DATETIME NOT NULL,
                                             reservation_team_Whether BIGINT NOT NULL,
                                             reservation_team_id BIGINT NULL,
                                             soccer_field_id BIGINT NOT NULL,
                                             FOREIGN KEY (soccer_field_id) REFERENCES soccer_field(id)
        );
    </update>
</mapper>
