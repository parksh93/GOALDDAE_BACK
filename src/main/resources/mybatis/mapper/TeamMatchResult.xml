   <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goalddae.repository.TeamMatchResultRepository">
    <update id="createTeamMatchResultTable" parameterType="java.lang.Long">
        CREATE TABLE team_match_result_#{teamId} (
                                            id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                            match_id BIGINT NOT NULL,
                                            home_score BIGINT,
                                            away_score BIGINT,
                                            away_team_id BIGINT NOT NULL
        );
    </update>
    <insert id="insertHomeTeamMatchResult" parameterType="com.goalddae.dto.manager.ManagerTeamMatchResultDTO">
        INSERT INTO team_match_result_${homeTeamId} (match_id, home_score, away_score, away_team_id)
        VALUES (#{matchId}, #{homeScore}, #{awayScore}, #{awayTeamId});
    </insert>
    <insert id="insertAwayTeamMatchResult" parameterType="com.goalddae.dto.manager.ManagerTeamMatchResultDTO">
        INSERT INTO team_match_result_${awayTeamId} (match_id, home_score, away_score, away_team_id)
        VALUES (#{matchId}, #{awayScore}, #{homeScore}, #{homeTeamId});
    </insert>
</mapper>
