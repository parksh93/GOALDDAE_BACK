junit:
  jupiter:
    execution:
      parallel:
        enabled: true
        mode:
          classes:
            default: concurrent
          default: concurrent
        config:
          strategy: dynamic
          dynamic:
            factor: 1